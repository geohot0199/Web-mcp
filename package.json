{
  "name": "orbit-webmcp-3d-kernel",
  "version": "2.0.0",
  "private": true,
  "description": "Agent-native WebMCP 3D modelling kernel: exact CSG booleans, freeform geometry, procedural modifiers, parametric node graphs, mesh import/export and physics validation. No human approval layer.",
  "type": "module",
  "scripts": {
    "check": "npm run lint && npm run test:kernel && npm run test:webmcp && npm run test:adversarial",
    "lint": "node --check js/geom.js && node --check js/primitives.js && node --check js/csg.js && node --check js/modifiers.js && node --check js/io.js && node --check js/physics.js && node --check js/nodegraph.js && node --check js/scene.js && node --check js/webmcp.js && node --check js/viewport-decor.js && node --check js/app.js",
    "test:kernel": "node scripts/test-kernel.mjs",
    "test:webmcp": "node scripts/test-webmcp.mjs",
    "test:adversarial": "node scripts/test-adversarial.mjs",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0"
  },
  "keywords": [
    "webmcp",
    "3d",
    "csg",
    "agent",
    "mcp",
    "geometry",
    "parametric"
  ],
  "devDependencies": {
    "three": "^0.164.0"
  }
}
